# Ensure package integrity checks
package-lock=true
audit=false

# Use strict SSL
strict-ssl=true

# Increase timeout for Flow EVM packages
fetch-timeout=300000
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000

# Legacy peer deps (if needed for compatibility)
legacy-peer-deps=false
